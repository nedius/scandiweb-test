<?php

namespace Nedius\Models;

// now it's a dummy class, because we can't instantiate abstract class
class Product extends Products {
    public function validateType($description) {
        //do nothing ???
    }
}